import type { Metadata } from "next";
import { JetBrains_Mono, Space_Grotesk } from "next/font/google";
import "./globals.css";

const spaceGrotesk = Space_Grotesk({
  variable: "--font-space-grotesk",
  subsets: ["latin"],
});

const jetBrainsMono = JetBrains_Mono({
  variable: "--font-jetbrains-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  metadataBase: new URL("https://cyberreflex.com"),
  title: "CyberReflex | Instant Website Security Scans",
  description:
    "Scan any website for TLS issues, weak headers, exposed files, risky cookies, and DNS misconfigurations in one human-readable report.",
  openGraph: {
    title: "CyberReflex",
    description:
      "Instant website security scans with AI-assisted remediation guidance.",
    siteName: "CyberReflex",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${spaceGrotesk.variable} ${jetBrainsMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
